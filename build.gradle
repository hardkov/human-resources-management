subprojects {
    apply plugin: 'idea'

    group = 'com.agh.hr'
    version = '1.0.0'

    repositories {
        mavenCentral()
    }
}

task 'run' (type: Exec) {
    dependsOn 'build', ':spring-app:bootRun'
}
