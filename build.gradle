subprojects {
    apply plugin: 'idea'

    group = 'com.agh.hr'
    version = '1.0.0'

    repositories {
        mavenCentral()
    }
}

task build {
    doLast {
        ':spring-app:build'
    }
}

task 'run' (type: Exec) {
    dependsOn ':spring-app:build', ':spring-app:bootRun'
}
