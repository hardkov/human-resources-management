import ApplicationStatus from './ApplicationStatus';

interface SubmitBonusApplicationData {
  content: string;
  place: string;
  status: ApplicationStatus;
  money: 0;
}

export default SubmitBonusApplicationData;
